<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>User Interface Review</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= favicon_link_tag 'favicon.png' %>
    <%= stylesheet_link_tag 'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
  </head>
  <body>
    <nav>
      <div class="logo">
        <%= link_to image_tag('logo-white.svg'), root_path %>
      </div>
      <div class="search">
        <form action="/">
          <input type="text" name="search" placeholder="Search" value="<%= "#{@search}" %>"/>
        </form>
      </div>
      <div class="add">
        <%= link_to 'Add ref', new_ref_path %>
      </div>
    </nav>
    <main>
      <%= yield %>
      </main>
    <footer>
      <br/>
      <%= link_to 'Logout', destroy_user_session_path, method: :delete, class: 'btn btn-sm' %>
    </footer>
  </body>
</html>

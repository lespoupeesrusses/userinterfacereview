<% content_for :title, @user %>
<h1><%= @user %></h1>
<%= simple_form_for @user do |f| %>
  <%= f.input :first_name %>
  <%= f.input :last_name %>
  <%= f.input :email %>
  <%= f.submit 'Save', class: 'btn btn-primary' %>
<% end %>
